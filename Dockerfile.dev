# services/workfolio/Dockerfile.dev
# Vite dev server, same simple pattern: root user, host-mounted code.

FROM node:20

ENV NODE_ENV=development

WORKDIR /app

CMD ["npm", "run", "dev", "--", "--host", "--no-open"]
